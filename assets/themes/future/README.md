# Future Theme Slot

在这个目录中添加新的主题目录，例如：

- `assets/themes/cyber/theme.json`
- `assets/themes/cyber/*.wav`
- `assets/themes/cyber/*.png`

主题文件至少需要提供 `colors`、`pieces`、`audio` 结构。缺失项会自动回退到 `default` 主题。
